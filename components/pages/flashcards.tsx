import styles from '@/components/styles/pages.module.css'

export default function FlashcardPage() {
    return (
        <div className={styles.pageContainer}>
            <h1 className= {styles.title}>Flashcards page</h1>
        </div>
    );
}